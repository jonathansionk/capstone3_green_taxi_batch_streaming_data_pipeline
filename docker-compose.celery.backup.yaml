---
x-airflow-common:
  &airflow-common

  build:
    context: .
    dockerfile: Dockerfile

  image: jcdeah-airflow:3.3.0

  env_file:
    - .env

  environment:
    &airflow-common-env

    # ==========================================================
    # AIRFLOW EXECUTOR
    # ==========================================================
    AIRFLOW__CORE__EXECUTOR: CeleryExecutor

    # ==========================================================
    # GOOGLE CLOUD CONFIGURATION
    # ==========================================================

    # Project ID dari file .env.
    # Docker Compose akan berhenti jika variabel belum diisi.
    GCP_PROJECT_ID: "${GCP_PROJECT_ID:?GCP_PROJECT_ID belum diisi di file .env}"

    # Default project yang digunakan library Google Cloud.
    GOOGLE_CLOUD_PROJECT: "${GCP_PROJECT_ID:?GCP_PROJECT_ID belum diisi di file .env}"
    GCLOUD_PROJECT: "${GCP_PROJECT_ID:?GCP_PROJECT_ID belum diisi di file .env}"
    CLOUDSDK_CORE_PROJECT: "${GCP_PROJECT_ID:?GCP_PROJECT_ID belum diisi di file .env}"

    # Connection ID yang dipanggil oleh DAG.
    AIRFLOW_GCP_CONN_ID: "${AIRFLOW_GCP_CONN_ID:-google_cloud_default}"

    # Lokasi Application Default Credentials di dalam container.
    GOOGLE_APPLICATION_CREDENTIALS: "/home/airflow/.config/gcloud/application_default_credentials.json"
    CLOUDSDK_CONFIG: "/home/airflow/.config/gcloud"

    # Connection Airflow google_cloud_default.
    AIRFLOW_CONN_GOOGLE_CLOUD_DEFAULT: >-
      {"conn_type":"google_cloud_platform","extra":{"project":"${GCP_PROJECT_ID:?GCP_PROJECT_ID belum diisi di file .env}"}}

    # ==========================================================
    # AIRFLOW CORE CONFIGURATION
    # ==========================================================
    AIRFLOW__CORE__AUTH_MANAGER: airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager

    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: >-
      postgresql+psycopg2://airflow:airflow@postgres/airflow

    AIRFLOW__CELERY__RESULT_BACKEND: >-
      db+postgresql+psycopg2://airflow:airflow@postgres/airflow

    AIRFLOW__CELERY__BROKER_URL: redis://:@redis:6379/0

    AIRFLOW__CORE__FERNET_KEY: "${FERNET_KEY}"

    AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
    AIRFLOW__CORE__LOAD_EXAMPLES: "false"

    AIRFLOW__CORE__EXECUTION_API_SERVER_URL: >-
      http://airflow-apiserver:8080/execution/

    AIRFLOW__API_AUTH__JWT_SECRET: >-
      ${AIRFLOW__API_AUTH__JWT_SECRET:-airflow_jwt_secret}

    AIRFLOW__API_AUTH__JWT_ISSUER: >-
      ${AIRFLOW__API_AUTH__JWT_ISSUER:-airflow}

    AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: "true"

    _PIP_ADDITIONAL_REQUIREMENTS: >-
      ${_PIP_ADDITIONAL_REQUIREMENTS:-}

    AIRFLOW_CONFIG: "/opt/airflow/config/airflow.cfg"

  # ==========================================================
  # SHARED VOLUMES
  # ==========================================================
  volumes:
    - ${AIRFLOW_PROJ_DIR:-.}/dags:/opt/airflow/dags
    - ${AIRFLOW_PROJ_DIR:-.}/logs:/opt/airflow/logs
    - ${AIRFLOW_PROJ_DIR:-.}/config:/opt/airflow/config
    - ${AIRFLOW_PROJ_DIR:-.}/plugins:/opt/airflow/plugins

    # Mount konfigurasi gcloud Windows ke container Airflow.
    - "${APPDATA}/gcloud:/home/airflow/.config/gcloud:ro"

  user: "${AIRFLOW_UID:-50000}:0"

  depends_on:
    &airflow-common-depends-on

    redis:
      condition: service_healthy

    postgres:
      condition: service_healthy


services:

  # ============================================================
  # POSTGRESQL
  # ============================================================
  postgres:
    image: postgres:16

    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: airflow

    volumes:
      - postgres-db-volume:/var/lib/postgresql/data

    healthcheck:
      test:
        - CMD
        - pg_isready
        - -U
        - airflow
      interval: 10s
      retries: 5
      start_period: 5s

    restart: always


  # ============================================================
  # REDIS
  # ============================================================
  redis:
    image: redis:7.2-bookworm

    expose:
      - 6379

    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      interval: 10s
      timeout: 30s
      retries: 50
      start_period: 30s

    restart: always


  # ============================================================
  # AIRFLOW API SERVER
  # ============================================================
  airflow-apiserver:
    <<: *airflow-common

    command: api-server

    ports:
      - "8090:8080"

    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - http://localhost:8080/api/v2/monitor/health
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    restart: always

    depends_on:
      <<: *airflow-common-depends-on

      airflow-init:
        condition: service_completed_successfully


  # ============================================================
  # AIRFLOW SCHEDULER
  # ============================================================
  airflow-scheduler:
    <<: *airflow-common

    command: scheduler

    healthcheck:
      test:
        - CMD-SHELL
        - >-
          airflow jobs check
          --job-type SchedulerJob
          --hostname "$${HOSTNAME}"
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    restart: always

    depends_on:
      <<: *airflow-common-depends-on

      airflow-init:
        condition: service_completed_successfully


  # ============================================================
  # AIRFLOW DAG PROCESSOR
  # ============================================================
  airflow-dag-processor:
    <<: *airflow-common

    command: dag-processor

    healthcheck:
      test:
        - CMD-SHELL
        - >-
          airflow jobs check
          --job-type DagProcessorJob
          --hostname "$${HOSTNAME}"
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    restart: always

    depends_on:
      <<: *airflow-common-depends-on

      airflow-init:
        condition: service_completed_successfully


  # ============================================================
  # AIRFLOW CELERY WORKER
  # ============================================================
  airflow-worker:
    <<: *airflow-common

    command: celery worker

    healthcheck:
      test:
        - CMD-SHELL
        - >-
          celery
          --app airflow.providers.celery.executors.celery_executor.app
          inspect ping
          -d "celery@$${HOSTNAME}"
          ||
          celery
          --app airflow.executors.celery_executor.app
          inspect ping
          -d "celery@$${HOSTNAME}"
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    environment:
      <<: *airflow-common-env
      DUMB_INIT_SETSID: "0"

    restart: always

    depends_on:
      <<: *airflow-common-depends-on

      airflow-apiserver:
        condition: service_healthy

      airflow-init:
        condition: service_completed_successfully


  # ============================================================
  # AIRFLOW TRIGGERER
  # ============================================================
  airflow-triggerer:
    <<: *airflow-common

    command: triggerer

    healthcheck:
      test:
        - CMD-SHELL
        - >-
          airflow jobs check
          --job-type TriggererJob
          --hostname "$${HOSTNAME}"
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    restart: always

    depends_on:
      <<: *airflow-common-depends-on

      airflow-init:
        condition: service_completed_successfully


  # ============================================================
  # AIRFLOW INITIALIZATION
  # ============================================================
  airflow-init:
    <<: *airflow-common

    entrypoint: /bin/bash

    command:
      - -c
      - |
        if [[ -z "${AIRFLOW_UID}" ]]; then
          echo
          echo -e "\033[1;33mWARNING: AIRFLOW_UID belum dikonfigurasi.\e[0m"
          echo "Menggunakan user ID dari sistem."
          echo
          export AIRFLOW_UID=$$(id -u)
        fi

        one_meg=1048576

        mem_available=$$((
          $$(getconf _PHYS_PAGES) *
          $$(getconf PAGE_SIZE) /
          one_meg
        ))

        cpus_available=$$(grep -cE 'cpu[0-9]+' /proc/stat)

        disk_available=$$(df / | tail -1 | awk '{print $$4}')

        warning_resources="false"

        if (( mem_available < 4000 )); then
          echo
          echo -e "\033[1;33mWARNING: Memori Docker kurang dari 4 GB.\e[0m"
          echo "Memori tersedia: $$(numfmt --to iec $$((mem_available * one_meg)))"
          echo
          warning_resources="true"
        fi

        if (( cpus_available < 2 )); then
          echo
          echo -e "\033[1;33mWARNING: CPU Docker kurang dari 2 CPU.\e[0m"
          echo "CPU tersedia: $${cpus_available}"
          echo
          warning_resources="true"
        fi

        if (( disk_available < one_meg * 10 )); then
          echo
          echo -e "\033[1;33mWARNING: Disk Docker kurang dari 10 GB.\e[0m"
          echo "Disk tersedia: $$(numfmt --to iec $$((disk_available * 1024)))"
          echo
          warning_resources="true"
        fi

        if [[ $${warning_resources} == "true" ]]; then
          echo
          echo "Resource Docker belum memenuhi rekomendasi Airflow."
          echo
        fi

        echo
        echo "Membuat direktori Airflow jika belum tersedia..."
        echo

        mkdir -v -p /opt/airflow/{logs,dags,plugins,config}

        echo
        echo "Versi Airflow:"
        echo

        /entrypoint airflow version

        echo
        echo "Isi shared volume:"
        echo

        ls -la /opt/airflow/{logs,dags,plugins,config}

        echo
        echo "Membuat konfigurasi default Airflow jika belum tersedia..."
        echo

        /entrypoint airflow config list >/dev/null

        echo
        echo "Mengatur ownership direktori Airflow..."
        echo

        chown -R "${AIRFLOW_UID:-50000}:0" /opt/airflow/

        chown -v -R \
          "${AIRFLOW_UID:-50000}:0" \
          /opt/airflow/{logs,dags,plugins,config}

        echo
        echo "Airflow initialization selesai."
        echo

    environment:
      <<: *airflow-common-env

      _AIRFLOW_DB_MIGRATE: "true"
      _AIRFLOW_WWW_USER_CREATE: "true"

      _AIRFLOW_WWW_USER_USERNAME: >-
        ${_AIRFLOW_WWW_USER_USERNAME:-airflow}

      _AIRFLOW_WWW_USER_PASSWORD: >-
        ${_AIRFLOW_WWW_USER_PASSWORD:-airflow}

      _PIP_ADDITIONAL_REQUIREMENTS: ""

    user: "0:0"


  # ============================================================
  # AIRFLOW CLI
  # ============================================================
  airflow-cli:
    <<: *airflow-common

    profiles:
      - debug

    environment:
      <<: *airflow-common-env
      CONNECTION_CHECK_MAX_COUNT: "0"

    command:
      - bash
      - -c
      - airflow

    depends_on:
      <<: *airflow-common-depends-on


  # ============================================================
  # FLOWER
  # ============================================================
  flower:
    <<: *airflow-common

    command: celery flower

    profiles:
      - flower

    ports:
      - "5555:5555"

    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - http://localhost:5555/
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    restart: always

    depends_on:
      <<: *airflow-common-depends-on

      airflow-init:
        condition: service_completed_successfully


volumes:
  postgres-db-volume: